# =========================================
#  DESACTIVAMOS TODA CONEXIÓN A MYSQL
# =========================================

spring.datasource.url=jdbc:mysql://localhost:3306/dummy
spring.datasource.username=dummy
spring.datasource.password=dummy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# No inicializar nada
spring.sql.init.mode=never
spring.datasource.initialization-mode=never

# Evita que Hibernate intente crear o validar tablas
spring.jpa.hibernate.ddl-auto=none
spring.jpa.generate-ddl=false
spring.jpa.show-sql=false

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Desactiva validaciones del schema
spring.jpa.properties.hibernate.hbm2ddl.auto=none

# Log mínimo
logging.level.org.hibernate=ERROR

# Puerto donde escucha el backend
server.port=8083

